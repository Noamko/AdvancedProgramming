{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cppbuild",
            "label": "Build Project",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "-std=c++11",
                "-pthread",
                "${workspaceFolder}/*.cpp",
                "-o",
                "${fileDirname}/a.out"
            ],
            "options": {
                "cwd": "/usr/bin"
            },
            "group": "build",
            "detail": "compiler: /usr/bin/g++"
        }
    ]
}